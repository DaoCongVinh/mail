{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "devCommand": "npm start",
  "env": {
    "REACT_APP_API_URL": {
      "description": "Backend API URL (e.g., https://your-backend.onrender.com/api)"
    }
  }
}
